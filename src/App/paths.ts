export const home = () => `/home`;
export const welcome = () => `/welcome`;

export const schedule = {
  home: () => `${home()}/schedule`,
};

export const discover = {
  home: () => `${home()}/discover`,
};